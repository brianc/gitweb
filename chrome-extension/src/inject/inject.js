var tid = setInterval(function() {
  //$('button.merge-branch-action').hide();
  var $button = $('button.merge-branch-action');
  $button.html('<span class="octicon octicon-thumbsup"></span> Rebase + Merge');
}, 100);
